Project Requirements:
1. Main subheader (AI Automation Engineer | Customer Advocate | Team Lead) line breaking logic should be as follows: the  'Customer Advocate' and  'Team Lead' must only be moved to a new line completely. Moving only 'Lead'
or only 'Advocate' is unacceptable. When any one of those items gets moved to a new line, the preceding '|' divider must disappear via the most reliable method, so that the first line doesn't get left ending with the divider.
2. Each contact  button, default ones, and in the burger menu, must have a light animation on hover: 
    - a displacement to the left by half it's width;
    - a 60bpm pulsating increase in size by 7%;
    - a 3D blend mode animation (see code example in \3D_blend_mode_animation.cssw).

3. All Skills section elements, such as '/html/body/main/div[1]/section/section[4]/div/div[1]' must get a dynamic gradient background on hover looping through these colors in the exact order:
#fb2c36, #f54900, #ff8904, #ffb900, #ffdf20, #497d00, #9ae600, #00a63e, #002c22, #009966, #005f78, #1c398e, #00a6f4, #312c85, #4d179a, #9810fa, #e12afb, #e60076, #861043, #ec003f. 
It may be applied as a separate animation for each element, or a single gradient with masking on all of the elements: whatever option is more reliable and easy to implement.
4. The codebase is optimized and all dependencies and libraries are updated to the latest stable versions that work together seamlessly. There are no elements, styles, files, or whatever else redundancies that don't serve a function or make the code clunky.
