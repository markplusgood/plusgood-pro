{
    "excludeTools": [
        "ShellTool(rm -rf)", // Excludes the 'rm -rf' command specifically
        "ShellTool(npm run dev)" //
        "ShellTool(npm run build)" //
        "ShellTool(yarn dev)" //
        "ShellTool(yarn build)" //
    ]
}
